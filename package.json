{
  "name": "chat-meteor",
  "version": "1.0.0",
  "description": "Chat with Meteor and React-Native",
  "author": "Luis-1896",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha",
    "run:mac": "MONGO_URL=mongodb://localhost:27017/versus meteor --settings ./settings/settings-development.json",
    "run:windows": "SET MONGO_URL=mongodb://localhost:27017/versus& meteor --settings ./settings/settings-development.json"
  },
  "dependencies": {
    "@babel/runtime": "^7.8.4",
    "bcrypt": "^4.0.1",
    "fs-extra": "^9.0.0",
    "meteor-node-stubs": "^1.0.0",
    "react": "^16.13.0",
    "react-dom": "^16.13.0"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.jsx",
      "server": "server/main.js"
    },
    "testModule": "tests/main.js"
  }
}
